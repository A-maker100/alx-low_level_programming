/**
 *_puts_recursion - Major function in this program
 * @s: string literal as parameter
 * Description: prints a string to std output
 * Return: void; no return value
 */
#include<string.h>
#include<stdio.h>
void _puts_recursion(char *s)
{
	puts(s);
}
