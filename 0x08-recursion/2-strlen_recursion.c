/**
 * _strlen_recursion- Major function in this program
 * @s: input string parameter
 * Description: returns length of input string
 * Return: int
 */
#include<string.h>

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
