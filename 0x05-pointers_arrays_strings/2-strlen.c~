/**
 * _strlen - Major function in this .c program
 * @s : char pointer parameter
 * Description : returns the length of a string
 * Return : length of given string
 */
#include<string.h>

int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
