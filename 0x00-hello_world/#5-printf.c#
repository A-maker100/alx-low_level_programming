#include<stdio.h>

/**
 * This program uses printf to send text to the output stream
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
/**
 * Returns 0 if it prints properly
 */
return (0);
}
