#include<stdlib.h>
#include<string.h>
/**
 *_atoi- Major function in this program
 *@s: string input as parameter
 * Description: converts a string to a number
 * Return: number version of a string
 */

int _atoi(char *s)
{
	return (atoi(s));
}
