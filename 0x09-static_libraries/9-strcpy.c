#include<string.h>

/**
 * _strcpy- function in this program
 * @dest: destination to copy to
 * @src: source to copy from
 * Description: copies src to dest
 * Return: dest after the copy is done
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
